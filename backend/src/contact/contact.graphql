type Contact {
	_id: ObjectID!
	userid: ObjectID!
	createdAt: Date!
	whoIsContact: String
	User: UserSafe
}

type Contacts {
	Existing: [Contact]
	Incoming: [Contact]
}

type Query {
	Contacts: [Contact]
	FindContacts(text: String!): Contacts
}
