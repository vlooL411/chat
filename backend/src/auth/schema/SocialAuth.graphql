type GoogleNetwork implements SocialNetwork {
	_id: ID!

	givenName: String!
	familyName: String!
	middleName: String!
	email: String!

	auth: Authentication!
}

type FacebookNetwork implements SocialNetwork {
	_id: ID!

	givenName: String!
	familyName: String!
	middleName: String!
	email: String!

	auth: Authentication!
}
