enum TokenType {
	authentication
	access
	refresh
}

type Authentication {
	accessToken: Token!
	refreshToken: Token!
}

type Query {
	Login(input: LoginInput): Authentication!
	Refresh(refreshToken: Token!): Authentication!
}

type Mutation {
	Register(input: RegisterInput!): UserSafe
}
