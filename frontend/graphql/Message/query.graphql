query messages(
	$chatid: ObjectID!
	$messageid: ObjectID
	$limit: Int
	$isIncoming: Boolean = false
) {
	Messages(
		chatid: $chatid
		messageid: $messageid
		limit: $limit
		isIncoming: $isIncoming
	) {
		Chat {
			...MessagesChat
			...LastMessage
		}
		InfoMore {
			...InfoMore
		}
	}
}
