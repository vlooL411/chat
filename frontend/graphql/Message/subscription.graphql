subscription addMessage {
	AddMessage {
		_id
		userid
		text
		createdAt
		isChange
	}
}

subscription swapMessage {
	SwapMessage {
		_id
		text
		isChange
	}
}

subscription deleteMessage {
	DeleteMessage {
		_id
	}
}
