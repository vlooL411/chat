import BlockInfo from 'components/Search/BlockInfo';
import { ReactElement } from 'react';

export default { title: 'Components/Search/BlockInfo' };

export const Default = (): ReactElement => <BlockInfo what='Founds chats 0' />;
